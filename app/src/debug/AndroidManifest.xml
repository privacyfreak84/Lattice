<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only overlay: registers the DebugBridgeReceiver so an automation agent can drive the app
     over `adb shell am broadcast` (send messages, read state). The manifest merger folds this in for
     debug variants only, so neither the receiver nor this entry ships in a release build. Exported so
     the `shell` uid (adb) can deliver to it; debug-only, so an exported custom-action receiver is fine. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="org.lattice.debug.DebugBridgeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.lattice.debug.SEND" />
                <action android:name="org.lattice.debug.SENDIMG" />
                <action android:name="org.lattice.debug.STATE" />
                <action android:name="org.lattice.debug.STORE" />
                <action android:name="org.lattice.debug.REACT" />
                <action android:name="org.lattice.debug.TYPING" />
                <action android:name="org.lattice.debug.SHAREAPK" />
                <action android:name="org.lattice.debug.HEAL" />
                <action android:name="org.lattice.debug.WEBPPROBE" />
                <action android:name="org.lattice.debug.WEBPCONV" />
                <action android:name="org.lattice.debug.WEBPCHECK" />
                <action android:name="org.lattice.debug.REQNOTIF" />
                <action android:name="org.lattice.debug.FLAGMSG" />
                <action android:name="org.lattice.debug.REVIEW" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
